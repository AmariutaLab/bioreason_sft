extends: h100_8b

# H100 analogue of v100_screen. Scores are not directly comparable to V100 if
# the base model differs, but the trace-source ranking should be comparable.
train:
  epochs: 1
  save_strategy: "no"
  save_total_limit: 1

eval:
  max_rows: 120
